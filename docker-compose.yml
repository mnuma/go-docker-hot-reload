version: '3.5'

services:
  app:
    build:
      context: .
      target: build
    volumes:
      - ./:/go/app
    command: realize start --run --no-config
    ports:
      - 1323:1323
  redis:
    image: redis:3.2.9
    ports:
      - "6379:6379"
    command: redis-server
